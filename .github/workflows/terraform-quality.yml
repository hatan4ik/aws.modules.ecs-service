name: terraform-quality

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    strategy:
      fail-fast: false
      matrix:
        directory:
          - .
          - modules/container-definition
          - modules/iam
          - modules/security-group
          - modules/autoscaling
          - examples/minimal
          - examples/complete
          - examples/private-platform
          - examples/load-balanced
          - examples/multiple-services
          - examples/bring-your-own
    uses: hatan4ik/terraform-pipelines/.github/workflows/terraform-quality.yml@838da070a7e33ed2911ad3768ba3d00cccc9a5d9 # v0.1.0
    with:
      working_directory: ${{ matrix.directory }}
      check_docs: true
